var fs = require('fs');
var inFile = fs.createReadStream("input.json", { encoding: "utf8" });
var outFile = fs.createWriteStream("output.img", { encoding: "utf8" });
out.write(str);
out.end();
/ currently the same as destroy() and destroySoon()



